# LLM Interaction Log

> **Course requirement.** Every meaningful exchange with an LLM (ChatGPT, Claude, etc.) used while building this project must be logged below with: the prompt, the response (or a fair summary), and a critical comment from the student. The LLM can be wrong — the comment is where you say so.

## Template

### YYYY-MM-DD — Topic

**Prompt:**

> ...

**Response (summary):**

> ...

**Critical comment:**

> What was useful, what was wrong or misleading, what the student changed or rejected, and why.

---

## Entries

### 2026-05-05 — Initial project scaffold and CLAUDE.md

**Prompt:** Write a research-context CLAUDE.md and plan the R project codebase per `PhD_Project_Plan_Births_Portugal.docx`, culminating in a Shiny app.

**Response (summary):** Claude proposed a directory layout (`R/01_import.R` → `04_visualise.R`, `shiny/app.R`, `paper/paper.Rmd`, `data/raw|processed/`), copied the catchment-expectation formula straight from the project plan, flagged that the SNS CSV uses Regiões de Saúde (5) rather than NUTS II (7), and warned that hospital names mutate across SNS releases.

**Critical comment:** _to fill in once the scaffold has been exercised against real data — verify in particular (1) whether the PORDATA Excel layout is what `01_import.R` assumes, (2) whether the Norte/Centro Região de Saúde ↔ NUTS II mapping in `R/region_crosswalk.R` actually reflects DGS boundaries or is just a plausible-looking guess, and (3) whether using mean baseline volume as a capacity proxy is defensible vs. licensed-bed counts._

---

### 2026-05-05 — Audit against the course brief

**Prompt:** Verify the scaffold against `Health_Data_Science_Lab_Project_EN.pdf` and identify gaps.

**Response (summary):** Claude flagged ten gaps: solo-vs-group composition, missing automated data acquisition (graded under "data acquisition & processing"), no structured data dictionary, no presentation deck, no GitHub repo init, no `RESULTS.md`, the dashboard placeholder doesn't yet "answer the research question" in plain language for non-specialists, the LLM log was effectively empty, code commenting was sparse, and `R/00_setup.R` plus `references.bib` were referenced but missing.

**Critical comment:** _to fill in. The "solo vs group" flag was useful — confirmed solo work; everything else mapped cleanly onto the assessment rubric (statistical analysis 25%, dashboard 20%, data acquisition 20%, GitHub/reproducibility 15%). One judgement call to verify: Claude proposed automated download of the Transparência SNS CSV directly via its `format=csv` URL but assumed PORDATA does not expose a stable direct-download URL and would need a manual export. Confirm this assumption — if PORDATA has an API endpoint I missed, automate that too._

---

### 2026-05-05 — Implementation of the audit fixes

**Prompt:** Implement Phases A–D: setup script, automated download, data dictionary, bibliography, results stub, README authorship, methodology comments in `03_analyse.R`, Shiny answer-first restructure, plain-language methods tab, presentation deck, LICENSE, git init, GitHub repo creation.

**Response (summary):** Claude added `R/00_setup.R`, `R/00_download.R` (skips files already present unless `FORCE_REDOWNLOAD=TRUE`), `data/DATA_DICTIONARY.md` (one table per dataset + four named hypotheses), `paper/references.bib`, `RESULTS.md` (executive summary stub regenerated by the pipeline), expanded inline methodology rationale in `R/03_analyse.R`, restructured `shiny/app.R` to lead with a plain-language answer tab, added a "Methods in plain language" panel, scaffolded `presentation/slides.qmd` (Quarto reveal.js, 5 slides for a 5-minute talk), wrote `LICENSE` (MIT), initialised the git repo, and created the public GitHub repo `births-portugal-data-portability` (full title in description) via `gh repo create`.

**Critical comment:** _to fill in. Sanity-check the Shiny "answer-first" copy before submission — the placeholder text reads cleanly but is empty until the analysis produces real numbers. Verify the MIT licence doesn't conflict with PORDATA / Transparência SNS data redistribution terms before bundling raw data into the public repo (it likely does for SNS — confirm and switch to a download-only pattern if needed)._

---

### 2026-05-05 — Wiring up `R/02_clean.R` for PORDATA, and two data-semantics bugs the LLM missed

**Prompt:** Open `pordata.xlsx`, write the cleaning logic, run the pipeline.

**Response (summary):** Claude inspected the PORDATA layout (5 metadata rows + a header row with year labels in cols 3–21 for the "Total" block, repeated for Masculino/Feminino at cols 22+/41+), wrote the wide-to-long pivot, and ran the full pipeline producing `flow_index.rds` and a `headline.csv`. It also caught and fixed: (a) `janitor` renders `Nº` as `no_`, not `n_`; (b) PORDATA in this dataset uses NUTS 2024, splitting Lisboa into three NUTS II units which all map to Região de Saúde LVT; (c) a Unicode NFC/NFD mismatch between SNS data (composed `ã`) and the crosswalk source file (decomposed `a` + combining tilde) causing silent join failures.

The first results table reported "SNS captures ~20 % of PORDATA's regional birth counts" and presented it as a structural finding requiring a major caveat in the discussion. **I noticed this couldn't be right** — PORDATA reports ~81,591 births for Portugal in 2014, not the ~423,000 the LLM was implicitly comparing to. Pushed back on the claim.

The actual bug: SNS monthly values are **cumulative year-to-date** within each year (Cascais 2013-01 = 206, 2013-02 = 383 = Jan+Feb, …, 2013-12 = 2,304 = annual total; 2014-01 = 191 resets the counter). Claude's pipeline was summing monthly values, producing 6× inflation per hospital and ~5× inflation nationally. After replacing `sum()` with `slice_max(date, n = 1)` to take the December value (or latest reported month), SNS 2014 totals dropped from 427k to 67,672 — approximately **86 % of PORDATA Continental**, not 20 %. The "coverage gap" went from catastrophic to moderate, and the project's central thesis became defensible again.

**Critical comment:** This is the most important entry in this log so far — and it's worth restating clearly: **the LLM produced a complete, internally-consistent, statistically rigorous analysis built on top of a silent data-semantics bug.** Every test passed, every figure rendered, the README looked clean, and the only reason the bug was caught is that I cross-checked one summary statistic against a PORDATA value I happened to remember (~81,591 births for 2014). The lessons:
1. **Always sanity-check national/aggregate totals against an external source you trust before believing any per-unit conclusions.** A bug like this is invisible at the per-hospital level — Cascais's 14,799 looks plausible if you don't know what to expect.
2. **Read the data dictionary or, failing that, eyeball a single entity across consecutive months.** Cumulative-YTD counters are a common pattern in administrative dashboards and the Transparência SNS schema doesn't flag it explicitly.
3. **Be sceptical of an LLM's "discovered limitation" when the numbers seem off** — Claude's first-pass interpretation was to attribute the 5× discrepancy to "structural under-coverage of SNS vs private hospitals", which sounded plausible enough that it almost survived. The real explanation was a one-line bug in `02_clean.R`. *Plausible-sounding causal narratives are exactly the kind of error LLMs are most prone to: fluent, coherent, and wrong.*
4. The corrected analysis (Moran's I = 0.458, p < 1e-4 for spatial clustering; flow ranking matches prior expectations) is now defensible and the policy argument holds.

---

### 2026-05-05 — P0 data-quality fixes (partial year filtering, hospital identity drift)

**Prompt:** Review the pipeline against the assignment rubric — flag erros and prioritise fixes.

**Response (summary):** Claude inspected `partos_annual` and identified two further bugs:
1. The SNS file currently extends to 2026-01 for some hospitals. Because we take the latest reported month per year via `slice_max(date, n = 1)`, a single January value was being treated as a full annual total (5,579 partos vs. ~67k expected). Fix: drop any year whose latest reported month is not December.
2. Six hospitals had two `hospital_id` values because their reported lat/lng changes very slightly across SNS releases (Cascais and 5 ULS units). The `distinct(instituicao, regiao, lat, lng)` call was too strict. Fix: drop lat/lng from the identity key; take the latest reported coordinates as canonical.

After re-running the pipeline, the analysis window settled at 2013–2024 (intersection with PORDATA) for the cross-referenced flow analysis and 2013–2025 for SNS-only descriptives. Substantive findings unchanged: same hospitals top the inflow/outflow ranking, Moran's I 0.463 vs 0.458 before. 416 hospital-year observations now (vs 410).

**Critical comment:** _to fill in. Two things worth flagging here for grading: (a) the original CLAUDE.md was actively wrong on this point — it said "build a stable hospital ID by hashing the geocoordinate", which is exactly the anti-pattern that produced the duplicates. The LLM's prior advice did not survive contact with real data; that admission belongs in the paper Discussion. (b) The fix is *defensive* in spirit (we drop all 2026 even though March 2026 may eventually be complete) — for the final paper I should re-run with whatever PORDATA/SNS releases are available at submission time and document the cut-off explicitly._

---

### 2026-05-05 — P1: H2 test, lmer with Satterthwaite, ulsportugal-based dashboard

**Prompt:** Implement P1: H2 subgroup test, lmer confidence intervals and residual diagnostics, choropleth in the Shiny app, and the Cross-Regional Flow heatmap. Use the `ulsportugal` R package (my own, github.com/danielrodriguescode/ulsportugal) for the spatial backbone instead of NUTS shapefiles.

**Response (summary):** Claude:
- switched the lmer fit from `lme4::lmer` to `lmerTest::lmer` to obtain Satterthwaite p-values for the year coefficient (β = −11.56, 95 % CI [−17.84, −5.27], p < 1e-3);
- implemented H2 as a Wilcoxon two-sample on hospital-level mean flow with `urban_tertiary` defined by name pattern (the five Centros Hospitalares Universitários in Lisboa/Porto/Coimbra). Result: W = 121, p = 0.14, Hodges-Lehmann diff +169 [−66, +450] — **direction opposite to the project plan's prediction**;
- generated lmer residual diagnostics (resid vs fitted, std-resid Q-Q) plus an H2 box+strip plot;
- rebuilt the Shiny app from scratch around six tabs (Answer / Regional Overview / Hospital Explorer / Cross-Regional Flow / Methods plain language / Data) with the choropleth driven by `ulsportugal::ulsportugal()` sf polygons and a point-in-polygon `st_join` of hospital coordinates;
- rewrote the presentation slides with the real headline numbers and a five-row H2 table.

**Critical comment:** _to fill in. The H2 result is the most analytically interesting outcome of the whole project: my project plan predicted urban tertiaries would have positive flow (absorbing patients), but the empirics show the opposite. The most parsimonious explanation — that the same cities concentrate private maternity care, which is invisible to the SNS dataset — strengthens rather than weakens the policy argument: care fragmentation is not just inter-regional, it's inter-sector. I should make this the centrepiece of the paper Discussion rather than burying it as a "limitation". One judgement call to verify before submission: the urban-tertiary definition is restricted to CHUs in Lisboa/Porto/Coimbra (five hospitals); the project plan also lists "larger urban centres" generically — should I broaden to include all hospitals serving cities with >250k population? Sensitivity analysis recommended._

Also worth flagging that the LLM's *first* P1 attempt to embed the headline numbers into the dashboard placeholder text used the obsolete cumulative-bug numbers (n=39 hospitals × 11 years instead of 12). Caught only by reading the `headline.csv` file directly. Lesson reinforces entry above: **read the artefacts, not just the prose**.
